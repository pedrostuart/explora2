# Porta em que o backend Nest vai subir
PORT=3000

# Dados de conexão com o MySQL (mesmo banco criado por backend/explora.sql)
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=root
DB_NAME=explora
